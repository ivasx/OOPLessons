public class Bird extends Animal{
    public Bird(String name, kindAnimal kind, int age, double weight, int hungerLevel, String location) {
        super(name, kind, age, weight, hungerLevel, location);
    }

    
}
